import { Link } from "react-router";

export default function Cart() {
    return (
        <div>
            <h1>Shopping Cart</h1>
            <Link to="/shop">Page Shop</Link>
        </div>
    );
}
